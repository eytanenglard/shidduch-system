// Shared types for split-view sub-components
// Re-exported from the consolidated shared types module

export type {
  BackgroundCompatibility,
  ScoreBreakdown,
  AiMatch,
  AiMatchMeta,
} from '../../types/shared';

export { toBackgroundCompatibility } from '../../types/shared';
