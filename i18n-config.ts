// i18n-config.ts

/**
 * תצורת הבינלאומיות (i18n) של האפליקציה.
 * קובץ זה מגדיר את השפות הזמינות ואת שפת ברירת המחדל.
 */

export const i18n = {
  // שפת ברירת המחדל. משמשת כ-fallback אם לא נמצאה שפה מתאימה.
  defaultLocale: 'he',
  
  // מערך של כל השפות הנתמכות באתר.
  // הוספת שפה חדשה כאן היא הצעד הראשון להפעלתה.
  locales: ['he', 'en'],
} as const; // 'as const' הופך את האובייקט לקריאה בלבד ומאפשר לנו ליצור טיפוסים מדויקים ממנו.

// יצירת טיפוס (Type) עבור השפות הזמינות.
// זה יעזור לנו לשמור על Type Safety בכל מקום שנשתמש בפרמטר השפה.
export type Locale = (typeof i18n)['locales'][number];